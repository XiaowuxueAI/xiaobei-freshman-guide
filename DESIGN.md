# DESIGN.md — 小北学长（福软迎新版）

设计系统与关键决策记录。这份文件描述**已经建成的这个世界**，不是建之前写下的理想稿。
产品事实在 `PRODUCT.md`，方向契约在 `.impeccable/surfaces/index-html.md`，自查证据在 `.impeccable/EVIDENCE.md`。

---

## 1. 世界与主张

**一句话**：这个站把「报到那天」排成一张行程表。

载体取自新生真实世界里的**车站行程单 / 时刻表**（我方七个候选载体里排第 3：前面是纸质报到流程单、校区导览路线图），
因为它同时装得下两件事：内容本身就是有先后顺序的（行前 → 报到当天 → 入学第一周 → 开学之后），
而新生真正需要的正是一个「我现在在哪一站、下一步做什么」。它被五处外来体系**提升**过（各取其纪律，不取其衣服）：

| 捐赠者 | 被借用的纪律 | 落到成品上 |
|---|---|---|
| 机架配线表（jackfield） | 状态用「记号」表达，不靠颜色；每个位置有稳定锚点 | 行程轴上的方块标记（下一站/普通）、装包进度的勾、`category.html#st-*` 每站一个永久锚点 |
| 剪辑台选片轨（cutting bench） | 你停下的地方会被重新标记 | 装包勾选跨会话保留，「已装 X / 42」在首页与攻略页同源同步 |
| 深海剖面下潜 | 一条纵向轴统辖整页 | 行程轴：一条 1px 竖线贯穿，所有站点挂在这条线上，而不是卡片并排 |
| Factory 唱片封套 | 大块留白、不重复贴标签 | 每个 band 一件事；标题下不再加 kicker/眉标；同类信息不写两遍 |
| 黑白单色电影海报 | 双色调克制 + 页脚票据式 billing block | 墨色 + 单一强调色的双色纪律；页脚是一张有发丝分隔线、等宽对齐的联系票据 |

**明确拒绝的默认套路**（brief 点名 + craft floor 禁令）：

- 蓝紫渐变科技风、居中大标题 + 三张等宽卡片、圆角阴影卡片海洋 —— 旧站在做，本版全部弃用；
- emoji 当图标、无意义装饰渐变、渐变文字、玻璃拟态装饰、硬投影、卡片左侧彩色竖条、mojibake 风格化 SVG 插画；
- 每节头顶一个 kicker、装饰性编号 01/02/03、hero 大数字（旧站的「100+ 攻略 / 20+ 问题 / 365天 陪伴」是装饰而非事实，已删）。

---

## 2. 颜色令牌（tokens）

命名按「在行程表这个世界里的职责」，不是按色值。全部在 OKLCH 思路下定色，但以 WCAG 比值验收。

### 纸面与场地（surfaces）

| Token | 值 | 职责 |
|---|---|---|
| `--paper-mist` | `#eaf0ec` | 页面底色。山雾青的最淡一档，**不是奶油纸**（刻意避开 AI 默认的暖米色纸面）。 |
| `--paper-window` | `#f7faf8` | 抬起来的面：面板、行程轴卡片、二维码票根、聊天气泡。 |
| `--paper-sunk` | `#dfe8e3` | 沉下去的场地：顶部行程头、清单区、表格头。 |
| `--paper-night` | `#16262a` | 页脚 billing block 的夜色底。 |

### 墨色（ink）

| Token | 值 | 对比度（AA 需 4.5） |
|---|---|---|
| `--ink-rail` | `#16262a` | 13.51 on mist（正文与标题） |
| `--ink-rail-soft` | `#3d5155` | 7.25 on mist（次级正文） |
| `--ink-faint` | `#55686c` | 5.07 on mist / 4.69 on sunk / 5.58 on window（标签、元信息） |
| `--ink-night` | `#e6ede9` | 13.13 on night |
| `--ink-night-soft` | `#a9bdb8` | 7.93 on night |

### 信号色（signals，一个强调色 + 两个只表意的记号色）

| Token | 值 | 只允许用在哪 |
|---|---|---|
| `--signal-depart` | `#b03a1f` 迎新横幅暖橙红 | 「下一站」标记、主行动按钮、正文里的行动链接。5.24 on mist，白字落在其上 6.05。**不用于装饰。** |
| `--signal-depart-deep` | `#8d2d15` | 上面那个的 hover / active。 |
| `--signal-arrived` | `#2c6a4d` 校园绿荫 | 只表示「已办 / 已完成」：装包勾选框、进度条、完成印章。 |
| `--signal-verify` | `#8a4a2e` 老校区砖红 | 只表示「提醒 / 待核对」：提醒块标签、页脚印章、备注编号。 |

### 分隔线（rules）

`--rule-hairline` 14% 墨、`--rule-strong` 34% 墨、`--rule-dashed` 26% 墨、`--rule-night` 夜色底上的 22% 浅线。
规则：**发丝线承担结构，阴影承担不了的事才用阴影**。全站没有一处卡片阴影；深度由面（paper-window / sunk / night）的色阶表达。

---

## 3. 排版

### 字体

- 正文与标题：`--type-body` = PingFang SC / HarmonyOS Sans SC / Microsoft YaHei UI / Microsoft YaHei / Noto Sans CJK SC / system-ui 栈。
- 数字、编号、表头、发票式标签：`--type-data` = ui-monospace / Cascadia Mono / Consolas / Menlo 栈，全部开 `font-variant-numeric: tabular-nums`。

**为什么一个 webfont 都不请求**：这是手机优先、校园弱网、无构建步骤的静态站（`PRODUCT.md` 原则 5）。
中文字体动辄数 MB，任何 webfont 都会让首屏在弱网下变慢；这里**不假装有一个装饰字体**，
而是让「行程表的声音」由结构承担：字号/字重/字距对比、等宽数字的严格列对齐、发丝线的表格感、
以及一个真正属于数据角色的等宽字体（craft floor 允许 mono 用于 code/data/measurement —— 时刻表就是数据）。

### 角色阶梯

| Token | 值（手机 → 桌面） | 用途 |
|---|---|---|
| `--step-display` | `clamp(2rem, 6.4vw, 3.5rem)` | 每页唯一的 h1 |
| `--step-title` | `clamp(1.5rem, 4.2vw, 2.125rem)` | band 标题 h2 |
| `--step-heading` | `clamp(1.1875rem, 2.6vw, 1.375rem)` | 面板标题、相位标题 h3 |
| `--step-lede` | `clamp(1rem, 2.2vw, 1.125rem)` | deck / 导语 |
| `--step-body` | `1rem` | 正文下限，不再往下 |
| `--step-small` | `0.875rem` | 次级正文、链接、说明 |
| `--step-data` | `0.8125rem` | 序号、表头、等宽数据 |
| `--step-micro` | `0.75rem` | 标签、脚注 |

规则：标题 `letter-spacing` 最多负到 `-0.022em`（远在 `-0.04em` 底线之内）；`text-wrap: balance` 给标题、`pretty` 给段落；
行高按字面而非统一比例：正文 1.75、等宽标签 1.4–1.6、展示标题 1.14；
正文阅读宽度 `--measure-read: 34em`（中文约 34 字/行，对应拉丁的 65–75ch）。

---

## 4. 间距与节奏

| Token | 值 | 用途 |
|---|---|---|
| `--space-tick` | 0.375rem | 标题与其内联元信息之间 |
| `--space-tight` | 0.625rem | 同一组内的行距 |
| `--space-row` | 1rem | 表行内距、组件内小间隔 |
| `--space-group` | 1.75rem | 组与组之间 |
| `--space-section` | `clamp(3.25rem, 7vw, 5.25rem)` | band 上下内距 |
| `--space-field` | `clamp(4.5rem, 11vw, 8rem)` | 大块留白（照片带、页脚上方） |

两条硬规则：**同一个标题之上留的空间永远大于之下**（用 `margin-top` 大于 `margin-bottom` 实现，不靠目测）；
同一段滚动里密度要有起伏 —— 密（九个站点表）→ 静（四张实景照片）→ 密（提醒行）→ 静（行动与页脚）。

---

## 5. 布局

- `--shell-max: 1140px`，`--shell-gutter: clamp(1rem, 4.2vw, 2.5rem)`；所有 band 内部只有一层 `.shell` 负责横向边界。
- 断点：`720px`（行程头三列、清单两列、照片带三列）、`860px`（表行变四列时间表、站点摘要四列）、
  `960px`（首屏左右分栏）、`1040px`（问询窗口右栏吸顶）、`1140px` 之外由 `--shell-max` 接管。
- 360px 起可用：所有 12 个页面/视口组合实测 `scrollWidth == clientWidth`，零横向溢出。

---

## 6. 组件规则

**行程头 `.ticket`** —— 页面顶部一张票面：三个字段（行程 / 报到日 / 口径），手机端竖排虚线分隔，桌面端三列发丝竖线分隔。底为 `--paper-sunk`，是页首那道色阶。字段值只用真实信息，「口径」那一栏永远指向学校官方通知。

**行程轴 `.rail`** —— 标志性组件。`<ol>` + 1px 竖线（`::before`）+ 每站一个 11px 方块标记。
`data-current="true"` 的站是实心强调色 + 「下一站」旗标；站点编号等宽对齐。
线是**唯一**做入场动画的东西（见 §7）。

**页面级行程轴 `.route-axis`** —— 首屏那条轴必须继续往下走，否则「一条纵向轴统辖整页」只是首屏的装饰。
首页九个站点与攻略页九个站点的**相位列表整体包在 `.route-axis` 里**：1px 竖线从上贯到下（首页实测 1654→3564px，
正好收在最后一行的底边），每个相位在轴上有一个 11px 方块，每条行程行从轴引一条 12px 发丝短线接过来
（行是挂在轴上的，不是并排列队）。相位标签（出发前 3 天 / 报到当天…）是**轴左侧页边列的刻度文字**，
不是标题上方的眉标 —— 这是 craft floor 的禁令，也是这一版专门改掉的地方。

**表行 `.route-row` / `.station`** —— 攻略不是卡片，是表行：`序号 | 站名 | 要点 | 标签`。
**行的「状态」由它所在相位的轴上刻度承担**（行前 / 报到当天 / 入学第 1 周 / 开学之后 就是这一行的状态），
因此不再单设一个状态列——同一件事不写两遍。桌面四列对齐、hover 只改背景与编号颜色；移动端堆叠但保留左侧序号与发丝线。
禁止把行改成卡片、禁止行内加阴影、禁止 hover 时缩放行内图片（图片不是动作目标）。

**站点详情 `.station`（`<details>`）** —— 原生 disclosure，键盘可用，展开时 `+` 变 `–`（**画出来的方块，不是 +/– 字形**）；
展开内容用 220ms 的 `opacity/translateY(-4px)` 入场（合成属性），不animate高度。
问题库的 `.kb__marker` 用同一套几何（横杠常在、竖杠展开时收起）。

**装包清单** —— 真 `<input type="checkbox">`（`appearance:none` 自绘 20×20 方框，外层 `<label>` 整行可点，行高 ≥36px）。
勾选态用绿荫底色 + 手绘勾（形状与颜色同时表意，不只靠颜色）。进度条用 `transform: scaleX()`。
存 `localStorage['fusoft.pack.v1']`，勾满 42 项出现「装包完成」砖红印章；首页读数与之同源。

**问询窗口 `.chat`** —— 下沉场地（`--paper-mist`）+ 浮起消息（`--paper-window`）+ 墨色用户气泡。
等待回答时是一行静态文字「小北正在翻问题库…」，**不是跳动的小圆点**；进度由正文流式出现承担。
流式期间 `aria-busy="true"`，结束后一次性让读屏器播报。
建议问题只保留最近一组（最多 3 条），不堆叠。

**关注票根 `.plate`** —— 二维码始终在白底票根上（保证扫码对比），因此票根文字即使在夜色页脚里也用墨色而不是夜色文字。
每个页面都有：导航 CTA `关注小北` → 页脚「关注小北学长」区块（抖音 + 微信，用老板新图）→ 页脚 billing block 里的号码。

**页脚 billing block `.billing`** —— 等宽标签 + 值 + 斜体注释的票据式表；「公众号 / 邮箱 / 学校官网」等
未确认字段**写「待补充」，不留白也不编造**。

**印章 `.stamp`** —— 砖红 2px 方框 + `rotate(-2.5deg)`，全站只在两处：页脚声明、清单勾满。
它是几何图形（真实世界里的印章），不是插画。

**标签 `.tag` / 按钮 `.action`** —— 标签是 1px 发丝方框、12px；按钮 44px 高、3px 圆角（票据世界用近直角，
刻意不用 12–16px 卡片圆角：这个体系里没有卡片）。`--edge-window: 10px` 只给真正的「面」。

---

## 7. 动效（一个被授权的时刻）

- **唯一的创作时刻**：首页行程轴。JS 先加 `.is-armed`（没有 JS 就完全不隐藏），进入视口后加 `.is-drawn`：
  轴线 `scaleY(0→1)` 900ms `--ease-arrive`，站点依次 45ms 错峰浮现。1.6s 兜底，观察器失效也会出现。
- 其余一律安静：hover 只改背景/颜色/下划线（并且包在 `@media (hover: hover) and (pointer: fine)` 里，
  避免触屏误触发）；按钮 `:active` `scale(0.98)`；`<details>` 展开 220ms 合成属性入场。
- 时长全部 ≤ 320ms（交互反馈），缓动用 `--ease-arrive: cubic-bezier(.23,1,.32,1)`，不用 `ease-in`。
- `prefers-reduced-motion: reduce`：所有动画降到 0.01ms，行程轴直接静态可见。

---

## 8. 浏览器表面（最便宜、也最常被跳过的一层）

`::selection` 用强调色底 + 白字；`caret-color` 用强调色；滚动条用发丝色细条；
`:focus-visible` 是 2px 强调色描边 + 2px offset（**从不移除焦点环**）；链接统一 `text-underline-offset: .28em`；
等宽数字开 `tabular-nums`；另附一份 `@media print`——报到当天可以真的打一张纸出来。

---

## 9. 无障碍与 SEO 规则

- 语义：每页一个 h1，标题层级不跳级；表行用 `<a>`、站点用 `<details><summary>`、清单用 `<fieldset><legend>`、联系信息用 `<dl>`。
- 键盘：skip 链接为首焦点；抽屉 Esc 关闭并把焦点交回触发按钮；`<details>`、按钮、链接全部可 Tab 到达。
- 名称：图标按钮有 `aria-label`；表单控件都有 `<label>`（抽屉/聊天的视觉隐藏标签用 `.visually-hidden`）。
- 对比：正文与占位文字 ≥4.5:1，大字/控件 ≥3:1，全部用实测合成背景验证（见 EVIDENCE.md §1）。
- SEO：每页独立 `title` / `description` / `canonical` / OG / twitter 卡；`robots.txt` + `sitemap.xml`；
  攻略正文与 32 条问答是**静态 HTML**（不是 JS 渲染），无 JS 也能读到全部内容；图片有 `alt`、`width/height`、`loading="lazy"`。
- 隐私：问询窗口不联网、不存储输入；装包勾选只写本机 `localStorage`，页面上明说。

---

## 10. 图片规则

- **预算**：单张照片 ≤300KB（webp，宽度 1400–1600），缩略图 600–800w 供 `srcset`，
  并在 600w→1400w 的跨度中间补 1000w/1200w 候选（否则手机端 DPR2 会去拉 1400w 的那张）；二维码**不缩放**只重编码（保扫得动）。
- **格式**：照片用 WebP（原图 MPO/JPEG 共 20.6MB → 全部资源 1.9MB）；二维码同时给 WebP 与 PNG（PNG 为兼容副本）。
- **构图**：校园照片带是 1 大 + 3 小的不对称编排，不是等宽卡片墙；图片本身不加 hover 缩放。
- **alt 政策**：内容图写实描述（「校园实景照片：楼体与绿化」），装饰图 `alt=""`（聊天头像），二维码写明「扫码关注/添加什么」。
- **出处（provenance）**：`campus-0N.webp` 由老板提供的校园照片压缩而来；`wechat-qr` / `douyin-qr` 由老板 2026-09-10 提供的新二维码重编码；
  `og-cover.jpg` 是**本版合成的分享封面**（campus-02 + 文字），不是真实宣传图，也不是学校素材；`favicon.svg`、`apple-touch-icon.png` 为自制矢量/合成图。
- 旧站引用的 `campus-05..11.jpg`、`freshman-checklist-01/02.jpg`、`detail.html` 在源码镜像里并不存在（线上也是 404/回退页），本版**全部没有引用**。

---

## 11. 关键决策记录

1. **弃用 `detail.html`，把 9 站完整攻略放进 `category.html`（静态 HTML）。** 旧站每个攻略都链到 `detail.html?type=…`，
   而线上从未部署该文件（实测所有路径都回退到首页）——这是真实的 404。新版把内容静态写进站点页，
   用 `<details>` 展开，`#st-<id>` 作为永久锚点，既修好断链，也让无 JS 与爬虫都能读到全文。
2. **删掉假数据式的统计卡**（100+ / 20+ / 365天 / 阅读量 2.4k）。这些是装饰性数字，不是可核实事实；
   `PRODUCT.md` 把「不得虚构」列为产品原则，替换为真实可数的「9 个站点 / 42 项清单 / 32 条问题库」。
3. **AI 咨询的知识库从 14 条扩到 32 条（其中 14 条原样保留），且每条都能溯源**：所有新增条目都是旧站已有正文的重新组合
   （费用确认、现场流程、门禁与用电、图书馆、社团招新、奖助学金等），没有一条新政策、新价格、新时间。
   交付前的内容对账发现旧站的「开学要带什么？」在整理时被合并掉了，已作为第 04 条补回（连原答案一起）。
4. **知识库以静态 `<details>` 清单形式写进 HTML，聊天界面从 DOM 读同一份内容。**
   单一数据源避免了「页面文案与脚本答案不一致」，同时白送了 SEO 与无 JS 可用性。
5. **匹配算法**：精确命中 + 关键词长度加权 + 问题双字组重叠打分，阈值 8 分以下走兜底回答并给 3 条最近问题；
   对「有没有空调」这类非原句表述能命中「宿舍有空调吗」。
6. **二维码不做任何缩放**：先按 700px 缩过一版，但 `cv2` 解码对**原图**同样失败，说明失败原因不是缩放；
   为稳妥仍改回原始分辨率重编码，并用 `pyzbar` 验证微信码可解出 `https://u.wechat.com/EBWbr4i1FsQqxKYxbHpVcd4`。
7. **报到当天只写真实存在的信息**：行程轴上的时间窗 08:00–18:00 来自旧站原文；每一步的**具体钟点没有来源，所以不画**，
   轴上改为「相位（行前/报到当天/入学第 1 周/开学之后）+ 现场流程 5 步」。
8. **`--signal-depart` 从 `#bf4327` 调到 `#b03a1f`**：原值在纸底上只有 4.49:1，差 0.01 未过 AA；
   新值在两个方向上（纸底 5.24、白字落在其上 6.05）都过线。
9. **没有为了「高级」上特效**：全站零阴影卡片、零渐变装饰、零毛玻璃、零 emoji 图标、零 kicker 眉标；
   层级由字号/字重/等宽对齐/留白/唯一强调色承担。
10. **旧站 8 个「长篇详情标题」（如「新生报到指南：材料、缴费和现场流程」）没有搬进界面**。
    它们是那个从未部署的 `detail.html` 的 H1，信息已在站名 + 正文首段 + 小节标题里完整存在；
    按「一件事不写两遍」的纪律，没有再加一个重复标签。其中的特征词（现场、准备、消费等）已并入各站的 `data-tags`，
    搜索仍能命中。
11. **导航当前页标记用「站位方块 + 强调色」**，而不是彩色竖条：自查时发现第一版用了 `inset 3px 0 0`，
    正是 craft floor 明令禁止的侧条纹，已改为方块标记（移动端）/ 2px 下划线（桌面端）。

---

## 12. 遗留问题（需要老板确认，我没有替你决定）

1. **学校名与地址口径冲突**：旧站原文是「福州软件职业技术学院 · 福建省福州市长乐区滨海大道 999 号 · 文岭校本部」，
   而老板口述是「福建（南平）某软件学院」，微信署名也写「福建 南平」。**本版原文照抄旧站、没有改**，
   因为我不想凭空改学校事实。请确认以哪个为准，我会一次性改齐（页面、结构化数据、sitemap、OG 文案）。
2. **抖音二维码未通过程序化解码验证**：`cv2.QRCodeDetector` 与 `pyzbar` 对**原图**都解不出（微信那张能解出）。
   大概率是抖音个人名片码的叠加图形导致，但请上线后用手机实扫一次确认。
3. **联系方式仍待补充**：公众号名称、邮箱、学校官网/招生入口链接，本站一律标注「待补充」，没有写猜测值。
4. **`canonical` / OG / JSON-LD / sitemap / robots 统一用项目生产域名** `https://fusoft-freshman-guide.pages.dev/`
   （不再用形如 `f173deaa.…pages.dev` 的部署预览别名 —— 那是每次部署都会变的 hash 域名）。
   如果迁到自定义域名，需要全局替换这 5 个文件里的域名。
5. **`_tools/` 与 `.impeccable/` 是开发资料，不是站点页面**：`_tools/` 里是图片流水线与自查脚本（可留档，也可直接删），
   `.impeccable/` 是设计过程档（方向契约、证据、截图）。部署时如想保持根目录干净，删掉这两个目录即可，
   站点只依赖 `*.html / css/ / js/ / images/ / robots.txt / sitemap.xml`。
6. **未能亲眼看渲染**：本次会话的模型不接受图片输入，视觉结论建立在像素与几何度量、真实交互测试、
   结构/链接检查上（见 `EVIDENCE.md`）。建议老板用手机与桌面各过一眼，尤其是首页首屏与清单页的手感。

---

## 13. 以后怎么改（变更指引）

- **加一个攻略站点**：在 `category.html` 里复制一个 `details.station` 块（含 `id="st-新id"` 与 `data-tags="关键词 空格分隔"`），
  并在首页对应的 `.phase` 里加一条 `.route-row`（`href="category.html#st-新id"`）。搜索会自动带上它，`data-tags` 就是搜索键。
- **加一条问答**：在 `ai-consult.html` 的问题库 `<details class="kb__item" data-kb data-kw="关键词,关键词" data-station="st-xxx">` 里加一条，
  聊天界面会自动读到——不要在 JS 里另存一份答案。
- **加一条装包项**：在 `category.html` 的某个 `<fieldset class="check-group">` 里加一个 `<label class="check-item"><input type="checkbox" data-pack="唯一id"><span>名称</span></label>`，
  总数与进度条会自动重算（首页显示的总数在 `index.html` 的读数与文案里，改总数时同步这两处）。
- **换图片**：按 §10 的预算处理，文件名不变直接覆盖；`images/campus-0N.webp`（1600/1400w）与其 `-800.webp` 要成对更新，保持 `srcset` 宽度描述符一致。
- **换调色板**：只改 `:root` 里的令牌，不要在组件里写死颜色；改完必须重新跑一遍对比度（`.impeccable/EVIDENCE.md` §1 的表格是验收基线）。
